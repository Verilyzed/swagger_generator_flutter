name: swagger_generator_flutter
description: "Generate Dart code from OpenAPI (Swagger) specifications."
version: 0.0.3+1
repository: https://github.com/Verilyzed/swagger_generator_flutter

environment:
  sdk: ^3.8.0

dependencies:
  build: ^4.0.0
  source_gen: ^4.0.0
  yaml: ^3.1.2
  json_annotation: ^4.12.0
  chopper: ^8.0.0

dev_dependencies:
  build_runner: ^2.15.0
  json_serializable: ^6.8.0
  chopper_generator: ^8.0.0
  lints: ^4.0.0
  test: ^1.25.0
